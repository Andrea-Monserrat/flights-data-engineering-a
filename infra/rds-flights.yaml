AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Deploys a minimal PostgreSQL 17 RDS instance for the ITAM data product architecture
  course (chapter 10). Provisions the DB subnet group, security group, credentials
  in Secrets Manager, and the db.t3.micro instance. Designed to be accessed from
  SageMaker Studio in the same VPC.

# ─────────────────────────────────────────────────────────────────────────────
# PARAMETERS
# ─────────────────────────────────────────────────────────────────────────────
Parameters:

  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: >
      VPC where RDS will be deployed. Must be the same VPC as the SageMaker
      Studio domain so Studio notebooks can reach port 5432.

  SubnetIds:
    Type: List<AWS::EC2::Subnet::Id>
    Description: >
      At least two private subnets in different Availability Zones. RDS requires
      a subnet group with subnets in at least two AZs even for single-AZ instances.

  DBName:
    Type: String
    Description: Name of the initial database to create inside the RDS instance.
    Default: flights

  DBUsername:
    Type: String
    Description: Master username for the PostgreSQL instance.
    Default: itam
    NoEcho: false

  DBPassword:
    Type: String
    Description: >
      Master password for the PostgreSQL instance. Stored in Secrets Manager —
      do not share or commit this value. Minimum 8 characters.
    MinLength: 8
    NoEcho: true

  SecretName:
    Type: String
    Description: >
      Secrets Manager secret name. Change this if you need to redeploy within
      the 7-day recovery window of a previously deleted secret.
    Default: itam/rds/flights/credentials

  CreateReadReplica:
    Type: String
    Description: >
      Set to true to provision a read replica (db.t3.micro) in the same VPC.
      Use the replica endpoint for ETL reads and the primary endpoint for CRUD writes.
      Doubles the hourly RDS cost (~$0.034/hr each).
    Default: 'false'
    AllowedValues:
      - 'true'
      - 'false'

# ─────────────────────────────────────────────────────────────────────────────
# CONDITIONS
# ─────────────────────────────────────────────────────────────────────────────
Conditions:

  EnableReadReplica: !Equals [!Ref CreateReadReplica, 'true']

# ─────────────────────────────────────────────────────────────────────────────
# RESOURCES
# ─────────────────────────────────────────────────────────────────────────────
Resources:

  RdsCredentialsSecret:
    Type: AWS::SecretsManager::Secret
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      Name: !Ref SecretName
      Description: Master credentials for the flights RDS PostgreSQL instance.
      SecretString: !Sub |
        {
          "username": "${DBUsername}",
          "password": "${DBPassword}",
          "dbname":   "${DBName}",
          "port":     "5432",
          "engine":   "postgres"
        }

  RdsSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Allow PostgreSQL access from the internet (course demo)
      VpcId: !Ref VpcId
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 5432
          ToPort: 5432
          CidrIp: 0.0.0.0/0
          Description: PostgreSQL public access for SageMaker PublicInternetOnly mode

  RdsSubnetGroup:
    Type: AWS::RDS::DBSubnetGroup
    Properties:
      DBSubnetGroupDescription: Subnet group for the ITAM flights RDS instance
      SubnetIds: !Ref SubnetIds

  RdsInstance:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      DBInstanceIdentifier: !Sub 'itam-flights-${AWS::AccountId}'
      DBInstanceClass: db.t3.micro
      Engine: postgres
      EngineVersion: '17.4'
      DBName: !Ref DBName
      MasterUsername: !Ref DBUsername
      MasterUserPassword: !Ref DBPassword
      AllocatedStorage: '20'
      StorageType: gp2
      MultiAZ: false
      PubliclyAccessible: true
      DBSubnetGroupName: !Ref RdsSubnetGroup
      VPCSecurityGroups:
        - !Ref RdsSecurityGroup
      BackupRetentionPeriod: 1
      StorageEncrypted: true
      AutoMinorVersionUpgrade: true

  RdsReadReplica:
    Type: AWS::RDS::DBInstance
    Condition: EnableReadReplica
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      DBInstanceIdentifier: !Sub 'itam-flights-replica-${AWS::AccountId}'
      DBInstanceClass: db.t3.micro
      SourceDBInstanceIdentifier: !Ref RdsInstance
      PubliclyAccessible: true
      VPCSecurityGroups:
        - !Ref RdsSecurityGroup
      AutoMinorVersionUpgrade: true

# ─────────────────────────────────────────────────────────────────────────────
# OUTPUTS
# ─────────────────────────────────────────────────────────────────────────────
Outputs:

  RdsEndpoint:
    Description: >
      Endpoint de la instancia primaria. Usar para CRUD: crear tablas, insertar datos.
    Value: !GetAtt RdsInstance.Endpoint.Address

  RdsPort:
    Description: PostgreSQL port (always 5432).
    Value: !GetAtt RdsInstance.Endpoint.Port

  RdsSecretArn:
    Description: ARN del secret en Secrets Manager con las credenciales de RDS.
    Value: !Ref RdsCredentialsSecret

  RdsSecretName:
    Description: Nombre del secret en Secrets Manager.
    Value: !Ref SecretName

  RdsInstanceId:
    Description: RDS instance identifier.
    Value: !Ref RdsInstance

  ConnectionString:
    Description: >
      SQLAlchemy connection string template (reemplaza <password> con el valor
      de Secrets Manager — nunca hardcodees credenciales en notebooks).
    Value: !Sub 'postgresql+psycopg2://${DBUsername}:<password>@${RdsInstance.Endpoint.Address}:5432/${DBName}'

  RdsReplicaEndpoint:
    Condition: EnableReadReplica
    Description: >
      Endpoint de la Read Replica. Usar para queries analíticas en DBeaver y Jupyter.
    Value: !GetAtt RdsReadReplica.Endpoint.Address

  RdsReplicaPort:
    Condition: EnableReadReplica
    Description: PostgreSQL port de la Read Replica (always 5432).
    Value: !GetAtt RdsReadReplica.Endpoint.Port

  ReplicaConnectionString:
    Condition: EnableReadReplica
    Description: SQLAlchemy connection string template para la Read Replica.
    Value: !Sub 'postgresql+psycopg2://${DBUsername}:<password>@${RdsReadReplica.Endpoint.Address}:5432/${DBName}'
